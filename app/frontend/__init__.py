"""
Streamlit UI under ``app.frontend``: navigation, themes, meeting views, charts, PDF export.

Uses other ``app`` packages for configuration, storage, and pipeline runs where the UI triggers processing.
"""
